#      .
#     / V\
#   / `  /
#  <<   |    External Postgres example for mautrix-zulip.
#  /    |    Disable bundled postgres and provide explicit connection fields.
#

postgres:
  enabled: false

database:
  postgres:
    host: postgres.example.com
    port: 5432
    user: mautrix_zulip
    password:
      value: replace_me
    database: mautrix_zulip
    sslMode: require

homeserver:
  domain: matrix.example.com
