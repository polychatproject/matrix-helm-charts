{{- if .Values.postgres.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "mautrix-telegram.postgresFullname" . }}
  labels:
    {{- include "mautrix-telegram.componentLabels" (dict "context" . "component" "postgres") | nindent 4 }}
spec:
  serviceName: {{ include "mautrix-telegram.postgresFullname" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "mautrix-telegram.componentSelectorLabels" (dict "context" . "component" "postgres") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mautrix-telegram.componentSelectorLabels" (dict "context" . "component" "postgres") | nindent 8 }}
      annotations:
        checksum/secret: {{ include (print $.Template.BasePath "/postgres-secret.yaml") . | sha256sum }}
    spec:
      {{- if .Values.postgres.podSecurityContext.enabled }}
      {{- $podSecurityContext := omit .Values.postgres.podSecurityContext "enabled" }}
      {{- with $podSecurityContext.seccompProfile }}
      {{- if ne .type "Localhost" }}
      {{- $podSecurityContext = merge (omit $podSecurityContext "seccompProfile") (dict "seccompProfile" (omit . "localhostProfile")) }}
      {{- end }}
      {{- end }}
      securityContext:
        {{- toYaml $podSecurityContext | nindent 8 }}
      {{- end }}
      containers:
        - name: postgres
          image: {{ include "mautrix-telegram.postgresImage" . }}
          imagePullPolicy: {{ .Values.postgres.image.pullPolicy }}
          {{- if .Values.postgres.securityContext.enabled }}
          securityContext:
            {{- toYaml (omit .Values.postgres.securityContext "enabled") | nindent 12 }}
          {{- end }}
          env:
            - name: POSTGRES_DB
              value: {{ include "mautrix-telegram.databasePostgresDatabase" . | quote }}
            - name: POSTGRES_USER
              value: {{ include "mautrix-telegram.databasePostgresUser" . | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "mautrix-telegram.postgresFullname" . }}
                  key: password
            # PGDATA in a subpath so postgres entrypoint's chmod succeeds
            # under OpenShift's arbitrary high UID (the volume mount is
            # owned root:fsGroup g+rwx; the subpath is created by postgres
            # at first run with its own ownership).
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - name: postgres
              containerPort: {{ .Values.postgres.service.port }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          {{- with .Values.postgres.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if not .Values.postgres.persistence.enabled }}
      volumes:
        - name: data
          emptyDir: {}
      {{- end }}
  {{- if .Values.postgres.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - {{ .Values.postgres.persistence.accessMode }}
        resources:
          requests:
            storage: {{ .Values.postgres.persistence.size }}
        {{- if .Values.postgres.persistence.storageClassName }}
        storageClassName: {{ .Values.postgres.persistence.storageClassName }}
        {{- end }}
  {{- end }}
{{- end }}
