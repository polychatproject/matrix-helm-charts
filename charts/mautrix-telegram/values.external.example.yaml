#      .
#     / V\
#   / `  /
#  <<   |    External Postgres example for mautrix-telegram.
#  /    |    Disable bundled postgres and provide explicit connection fields.
#

postgres:
  enabled: false

database:
  postgres:
    host: postgres.example.com
    port: 5432
    user: mautrix_telegram
    password:
      value: replace_me
    database: mautrix_telegram
    sslMode: require

homeserver:
  domain: matrix.example.com

telegram:
  apiID: 12345678
  apiHash: replace_with_telegram_api_hash
