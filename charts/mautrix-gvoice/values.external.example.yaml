#      .
#     / V\
#   / `  /
#  <<   |    External Postgres example for mautrix-gvoice.
#  /    |    Disable bundled postgres and provide explicit connection fields.
#

postgres:
  enabled: false

database:
  postgres:
    host: postgres.example.com
    port: 5432
    user: mautrix_gvoice
    password:
      value: replace_me
      # existingSecret: my-postgres-password
      # existingSecretKey: password
    database: mautrix_gvoice
    sslMode: require

homeserver:
  domain: matrix.example.com
