#      .
#     / V\
#   / `  /
#  <<   |    External Postgres example for mautrix-gmessages.
#  /    |    Disable bundled postgres and provide explicit connection fields.
#

postgres:
  enabled: false

database:
  postgres:
    host: postgres.example.com
    port: 5432
    user: mautrix_gmessages
    password:
      value: replace_me
    database: mautrix_gmessages
    sslMode: require

homeserver:
  domain: matrix.example.com
