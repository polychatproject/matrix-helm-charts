#      .
#     / V\
#   / `  /
#  <<   |    These are the default values for ntfy.
#  /    |    You should look at the values examples for what to actually apply.
#

nameOverride: ""
fullnameOverride: ""

host: ""

image:
  repository: binwiederhier/ntfy
  tag: "v2.17.0"
  pullPolicy: IfNotPresent

args:
  - serve
  - --cache-file
  - /var/cache/ntfy/cache.db

replicas: 1

service:
  port: 80

probes:
  readiness:
    enabled: true
    path: /v1/health
    port: http
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 3
    successThreshold: 1
    failureThreshold: 3
  liveness:
    enabled: true
    path: /v1/health
    port: http
    initialDelaySeconds: 15
    periodSeconds: 20
    timeoutSeconds: 3
    successThreshold: 1
    failureThreshold: 3

podSecurityContext:
  enabled: true
  seccompProfile:
    type: RuntimeDefault

securityContext:
  enabled: true
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: false
  capabilities:
    drop:
      - ALL
    add:
      - NET_BIND_SERVICE

persistence:
  enabled: true
  accessMode: ReadWriteOnce
  size: 1Gi
  storageClassName: ""

ingress:
  enabled: true
  # Leave empty to derive from release name, e.g. "<release>-ntfy-ingress".
  name: ""
  className: traefik
  path: /
  pathType: Prefix
  clusterIssuer: letsencrypt-prod
  tls:
    enabled: true
    secretName: ntfy-tls

config:
  extra: ""

serviceMonitor:
  labels: {}

resources: {}
